######################################################################
# Sovol Zero  -  PAUSE / CANCEL / END_PRINT (single source of truth)
#
# Include AFTER Macro.cfg. Remove (or do not define) PAUSE, CANCEL_PRINT,
# and END_PRINT in Macro.cfg  -  this file replaces Sovol OEM park macros.
#
# Chain: CANCEL_PRINT → _CANCEL_PRINT_CLEANUP (mainsail retract/fan steps)
#        + SDCARD_RESET_FILE / CLEAR_PAUSE. Heaters off locally  -  do not call
#        nested CANCEL_PRINT_BASE (rename chain breaks TURN_OFF_HEATERS).
######################################################################


[gcode_macro _PARK_SAFE]
description: Park XY clamped inside stepper soft limits (0.5 mm margin)
gcode:
    {% set x = params.X|float %}
    {% set y = params.Y|float %}
    {% set f = params.F|default(9000)|float %}
    {% set sx = printer.configfile.settings['stepper_x'] %}
    {% set sy = printer.configfile.settings['stepper_y'] %}
    {% set xmin = sx.position_min|float + 0.5 %}
    {% set xmax = sx.position_max|float - 0.5 %}
    {% set ymin = sy.position_min|float + 0.5 %}
    {% set ymax = sy.position_max|float - 0.5 %}
    {% if x < xmin %}
        {% set x = xmin %}
    {% elif x > xmax %}
        {% set x = xmax %}
    {% endif %}
    {% if y < ymin %}
        {% set y = ymin %}
    {% elif y > ymax %}
        {% set y = ymax %}
    {% endif %}
    G90
    G1 X{x} Y{y} F{f}


[gcode_macro _LIFT_Z_SAFE]
description: Relative Z lift clamped to stepper_z position_max
gcode:
    {% set lift = params.LIFT|default(10)|float %}
    {% set f = params.F|default(3000)|float %}
    {% set zmax = printer.configfile.settings['stepper_z'].position_max|float %}
    {% set cur = printer.gcode_move.position.z|float %}
    {% if cur + lift > zmax %}
        {% set lift = zmax - cur %}
    {% endif %}
    {% if lift > 0 %}
        G91
        G1 Z{lift} F{f}
        G90
    {% endif %}


[gcode_macro PAUSE]
description: Pause  -  back-left park, bed lowered 20 mm for access
rename_existing: PAUSE_BASE
gcode:
    {% if printer.pause_resume.is_paused == False %}
        {% set e_retract = printer['gcode_macro _global_var'].pause_park.e|default(1)|float %}
        {% set state = params.STATE if params.STATE is defined and 'filament_change' in params.STATE else 'normal' %}
        {% set sx = printer.configfile.settings['stepper_x'] %}
        {% set sy = printer.configfile.settings['stepper_y'] %}
        {% set park_x = 0.0 %}
        {% set park_y = sy.position_max|float - 0.5 %}

        {action_respond_info("Pause Print!")}
        PAUSE_BASE X={park_x} Y={park_y}
        M117 Pause Print!!!
        M104 S{printer.extruder.target}

        {% if state == 'normal' %}
            {% if (printer.extruder.temperature + 5 >= printer.extruder.target) and
                  (printer.extruder.temperature >= printer.configfile.settings['extruder'].min_extrude_temp) %}
                {% if printer['filament_switch_sensor filament_sensor'].enabled == True and
                      printer['filament_switch_sensor filament_sensor'].filament_detected == True %}
                    G91
                    G1 E-{e_retract} F300
                    G90
                {% endif %}
            {% endif %}
        {% elif state == 'filament_change' %}
            {% if (printer.extruder.temperature + 5 >= printer.extruder.target) and
                  (printer.extruder.temperature >= printer.configfile.settings['extruder'].min_extrude_temp) %}
                G91
                G1 E-10 F1500
                G1 E-20 F600
                M400
                G4 P3000
                G1 E-50 F300
                G90
            {% endif %}
        {% endif %}
        {% if printer.toolhead.homed_axes|lower == "xyz" %}
            _PARK_SAFE X={park_x} Y={park_y}
            _LIFT_Z_SAFE LIFT=20
        {% endif %}
    {% endif %}


[gcode_macro _CANCEL_PRINT_CLEANUP]
description: Mainsail cancel helpers without recursive CANCEL_PRINT_BASE call
gcode:
    {% set client = printer['gcode_macro _CLIENT_VARIABLE']|default({}) %}
    {% set retract = client.cancel_retract|default(5.0)|abs %}
    {% if printer['gcode_macro RESUME'].restore_idle_timeout|default(0)|float > 0 %}
        SET_IDLE_TIMEOUT TIMEOUT={printer['gcode_macro RESUME'].restore_idle_timeout}
    {% endif %}
    _CLIENT_RETRACT LENGTH={retract}
    SET_FAN_SPEED FAN=part_cooling_fan SPEED=0
    {client.user_cancel_macro|default("")}
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=idle_state VALUE=False
    SET_PAUSE_NEXT_LAYER ENABLE=0
    SET_PAUSE_AT_LAYER ENABLE=0 LAYER=0


[gcode_macro CANCEL_PRINT]
description: Cancel  -  heaters off first, safe park, aux fan cooldown
rename_existing: CANCEL_PRINT_BASE
gcode:
    {% set e_retract = printer['gcode_macro _global_var'].cancel_park.e|default(1)|float %}
    {% set e_mintemp = printer.configfile.settings['extruder'].min_extrude_temp %}

    # Always kill heat first  -  mainsail CANCEL_PRINT_BASE rename chain can skip
    # TURN_OFF_HEATERS when cancel is triggered by SD/on_error after a fault.
    TURN_OFF_HEATERS
    M104 S0
    M140 S0
    M141 S0
    CHAMBER_OFF

    _CANCEL_PRINT_CLEANUP
    SDCARD_RESET_FILE
    CLEAR_PAUSE

    {% if printer.toolhead.homed_axes|lower == "xyz" %}
        {% set sx = printer.configfile.settings['stepper_x'] %}
        {% set sy = printer.configfile.settings['stepper_y'] %}
        {% set park_x = ((sx.position_min|float + 0.5) + (sx.position_max|float - 0.5)) / 2 %}
        {% set park_y = sy.position_max|float - 0.5 %}
        G91
        {% if printer['filament_switch_sensor filament_sensor'].enabled == True and
              printer['filament_switch_sensor filament_sensor'].filament_detected == True %}
            {% if printer.extruder.temperature >= e_mintemp %}
                G1 E-{e_retract} F500
            {% endif %}
        {% endif %}
        _LIFT_Z_SAFE LIFT=10
        _PARK_SAFE X={park_x} Y={park_y}
    {% endif %}

    ALL_FAN_OFF
    M117 Cooling Nozzle...
    SET_FAN_SPEED FAN=rear_auxiliary_fan SPEED=1.0
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM=0 MAXIMUM=50 TIMEOUT=300
    SET_FAN_SPEED FAN=rear_auxiliary_fan SPEED=0

    M220 S100
    M221 S100
    SET_GCODE_VARIABLE MACRO=_global_var VARIABLE=filament_sensor_print VALUE=False
    M84 X Y Z E
    {action_respond_info("Cancel Print Success!")}


[gcode_macro END_PRINT]
description: Normal print end  -  safe park and cooldown
variable_state: 'normal'
gcode:
    {% set e_mintemp = printer.configfile.settings['extruder'].min_extrude_temp %}

    SET_SKEW CLEAR=1
    M117 Finish Print!!!
    {% if printer.toolhead.homed_axes|lower == "xyz" %}
        G91
        {% if printer['filament_switch_sensor filament_sensor'].enabled == True and
              printer['filament_switch_sensor filament_sensor'].filament_detected == True %}
            {% if printer.extruder.temperature >= e_mintemp %}
                G1 E-2 F2700
                G1 E-2 Z0.2 F2400
            {% endif %}
        {% endif %}
        _LIFT_Z_SAFE LIFT=10
        _PARK_SAFE X=115 Y=152
    {% endif %}

    ALL_FAN_OFF
    TURN_OFF_HEATERS

    M117 Cooling Nozzle...
    SET_FAN_SPEED FAN=rear_auxiliary_fan SPEED=1.0
    TEMPERATURE_WAIT SENSOR=extruder MINIMUM=0 MAXIMUM=50 TIMEOUT=300
    SET_FAN_SPEED FAN=rear_auxiliary_fan SPEED=0

    M220 S100
    M221 S100
    SET_GCODE_VARIABLE MACRO=_global_var VARIABLE=filament_sensor_print VALUE=False
    CLEAR_PAUSE
    M84 X Y Z E
    {action_respond_info("Finish Print!")}
